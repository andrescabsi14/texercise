import taskList from './data/taskList'

export default function(endpoint) {
  //   return fetch(endpoint).then(data => {
  //     taskList = data.json()
  //     return taskList
  //   })
  return taskList
}
